# 复制为 .env 后填入你的值：cp .env.example .env
# .env 已被 .gitignore 忽略，不会提交。主进程启动时通过 dotenv 自动加载。

# 必需：API Key（agent 大脑接 Claude）
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 可选：自定义 endpoint / 代理（Anthropic 兼容的 messages API）。
# 不填则用官方 https://api.anthropic.com。代理是否带 /v1 视其而定。
# ANTHROPIC_BASE_URL=https://your-proxy.example.com

# 可选：覆盖默认模型（默认 claude-sonnet-4-5）
# - 官方 claude id 见 pi-ai 表：claude-sonnet-4-5 / claude-opus-4-5 / claude-haiku-4-5-20251001
# - 用 Anthropic 兼容代理时，可直接填代理侧的模型名（非 claude 也行），
#   例如 mimo-v2.5，会以已知模型为「接线模板」发到你的 BASE_URL。
# MODEL_ID=claude-sonnet-4-5
